// Removed: the transactional FX gain/loss screen was dropped at the client's
// request. Exchange-rate management now lives in System Settings via
// components/screens/finance/exchange-rates-card.tsx. This file is intentionally
// left as an inert module and can be deleted.
export {};
